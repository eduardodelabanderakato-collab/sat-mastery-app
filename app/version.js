// The sha of the question data this build was published against. It keys the
// browser's copy of the bank, so a republished bank is picked up at once and
// an unchanged one is never downloaded twice. Stamped by tools/publish.py.
export const BANK_STAMP = "07b61f3409d5";
